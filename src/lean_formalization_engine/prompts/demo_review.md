Write the deterministic demo review artifacts for `{theorem_name}`.

Required files in `{output_dir}`:
- `walkthrough.md`
- `readable_candidate.lean`
- `error.md`

Compile status: `{compile_status}`

Objective:
- map the Lean proof back to the intended mathematical route
- keep the readable rewrite and error summary separate from `candidate.lean`
- produce the exact three review artifacts later proof turns and final review expect
