Wrote `candidate.lean` with the locked imports and theorem signature unchanged, plus a proof skeleton that defines the one-variable slice `A`, `qS`, and `dS`, proves the fixed-point identities `A x * qS x = r` and `dS x * A x = p0`, and records the remaining blocker explicitly: the provided context surface does not include the matrix norm/import machinery needed to invoke mathlib’s inverse-differentiability lemmas for `x ↦ (A x)⁻¹`.