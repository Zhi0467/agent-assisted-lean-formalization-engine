# Examples

- `inputs/zero_add.md` — a minimal theorem statement that the demo agent can formalize
- `run_zero_add_demo.py` — end-to-end demo that writes artifacts under `artifacts/runs/zero_add_demo/`
