# Examples

`inputs/zero_add.md` plus `run_zero_add_demo.py` is the first runnable path through the
scaffold. It is intentionally simple: the point is to prove out the workflow shape,
artifact persistence, and Lean compile loop before widening the theorem surface.
